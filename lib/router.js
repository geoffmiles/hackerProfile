Router.route('/',{name:'hackerProfile'})

/*
Router.map(function() {
	this.route('hackerProfile',{
		path: '/',
		template: 'hackerProfile',
		waitOn: function() {
			return Meteor.subscribe('hacker');
		},
		cache: true
	}
})

*/